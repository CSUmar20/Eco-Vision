# EcoVision currently targets the iOS Simulator only. Its API address is fixed
# to http://localhost:8000 in src/config/api.ts, so no mobile environment value
# is required for local simulator development.
